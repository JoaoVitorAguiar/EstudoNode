
module.exports = {
  add: (a, b) => a + b, // Addition
  subtract: (a, b) => a - b, // Subtraction
  multiply: (a, b) => a * b, // Multiplication
  divide: (a, b) => a / b, // Division
  power: (a, b) => Math.pow(a, b), // Power
  squareRoot: (a) => Math.sqrt(a), // Square root
  logarithm: (a) => Math.log(a), // Logarithm
  sine: (a) => Math.sin(a) // Sine
};
